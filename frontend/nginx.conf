server {
  listen 80;
  root /usr/share/nginx/html;
  location /api/ { proxy_pass http://backend:8000; client_max_body_size 3m; proxy_read_timeout 120s; }
  location / { try_files $uri /index.html; }
}
